rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Licenses are managed by Cloud Functions only — no client access
    match /licenses/{document=**} {
      allow read, write: if false;
    }
  }
}
